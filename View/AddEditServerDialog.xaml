<Window x:Class="CommandCenter.View.AddEditServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Add Custom Server" Height="360" Width="440"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="ToolWindow"
        Loaded="Window_Loaded">
    <!-- Settings -> a BuildSection tab's "+ Add Custom Server" (or a custom entry's "Edit") -
         collects a display name plus enough connection detail to build the LaunchArgument string
         passed to the client executable at launch time (see TabServerEntry.LaunchArgument).
         GameLaunching/IP:Port are the two command keywords the client exes actually understand -
         Raw command is an escape hatch for anything else, typed in directly. -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Server name" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Padding="4" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="Connection type" Margin="0,0,0,4"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,14">
            <RadioButton x:Name="GameLaunchingRadio" Content="GameLaunching" GroupName="Mode" Margin="0,0,16,0" IsChecked="True" Checked="Mode_Checked"/>
            <RadioButton x:Name="IpPortRadio" Content="IP:Port" GroupName="Mode" Margin="0,0,16,0" Checked="Mode_Checked"/>
            <RadioButton x:Name="RawRadio" Content="Raw command" GroupName="Mode" Checked="Mode_Checked"/>
        </StackPanel>

        <!-- GameLaunching/IP:Port: Host+Port compose the argument, e.g. "GameLaunching 1.2.3.4 8484". -->
        <StackPanel Grid.Row="4" x:Name="HostPortPanel" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Host / IP" Margin="0,0,0,4"/>
                    <TextBox x:Name="HostTextBox" Padding="4"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Port" Margin="0,0,0,4"/>
                    <TextBox x:Name="PortTextBox" Padding="4"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Raw command: the whole argument is typed in directly - Host/Port above are unused. -->
        <StackPanel Grid.Row="4" x:Name="RawPanel" Margin="0,0,0,14" Visibility="Collapsed">
            <TextBlock Text="Launch argument" Margin="0,0,0,4"/>
            <TextBox x:Name="RawTextBox" Padding="4"/>
            <TextBlock Text="Passed to the client executable exactly as typed." Foreground="#FF888888" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button x:Name="ConfirmButton" Content="Save" Width="90" Padding="6,5" Margin="0,0,8,0"
                    IsDefault="True" Click="Confirm_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Padding="6,5"
                    IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
