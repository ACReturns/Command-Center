<Window x:Class="CommandCenter.View.RenameServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Rename Server" Height="160" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="ToolWindow"
        Loaded="Window_Loaded">
    <!-- Server Status → a custom group's "Rename" button. Only touches the group's display Title -
         its underlying json file name is left alone (see ServerStatusViewModel.RenameServer). -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Server name" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Padding="4" Margin="0,0,0,14"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RenameButton" Content="Rename" Width="90" Padding="6,5" Margin="0,0,8,0"
                    IsDefault="True" Click="Rename_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Padding="6,5"
                    IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
