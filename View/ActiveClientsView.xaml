<UserControl x:Class="CommandCenter.View.ActiveClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="740" d:DesignHeight="560">
    <UserControl.Resources>
        <DataTemplate x:Key="ClientTemplate">
            <Border Background="#FFF7F7F7" BorderBrush="#FFD0D0D0" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding TabTitle}" FontWeight="SemiBold" FontSize="14"/>
                        <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="#FF555555">
                            <Run Text="Server: "/><Run Text="{Binding ServerDisplayName, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock FontSize="12" Foreground="#FF555555">
                            <Run Text="Version: "/><Run Text="{Binding VersionNumber, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="#FF888888">
                            <Run Text="{Binding ExecutableName, Mode=OneWay}"/><Run Text=" - PID "/><Run Text="{Binding Pid, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <Button Grid.Column="1" Content="Close" Padding="12,5" VerticalAlignment="Center"
                            Command="{Binding DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding}"/>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Active Clients" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#FF666666"
                       Text="{Binding Clients.Count, StringFormat='{}{0} running'}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Clients}" ItemTemplate="{StaticResource ClientTemplate}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
