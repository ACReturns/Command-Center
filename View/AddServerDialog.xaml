<Window x:Class="CommandCenter.View.AddServerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Add New Server" Height="220" Width="460"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="ToolWindow">
    <!-- Server Status → "Add New Server": collects a display name and a worlds json to load it
         from. The file itself is copied into the Servers folder by the caller (see
         ServerStatusViewModel.AddServer) once this returns true - this dialog only picks inputs. -->
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Server name" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameTextBox" Padding="4" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="Server status json" Margin="0,0,0,4"/>
        <DockPanel Grid.Row="3" Margin="0,0,0,14">
            <Button DockPanel.Dock="Right" Content="Browse..." Width="80" Padding="6,4" Margin="8,0,0,0" Click="Browse_Click"/>
            <TextBox x:Name="FilePathTextBox" IsReadOnly="True" Padding="4" VerticalContentAlignment="Center"/>
        </DockPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
            <Button x:Name="AddButton" Content="Add" Width="90" Padding="6,5" Margin="0,0,8,0"
                    IsDefault="True" Click="Add_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Padding="6,5"
                    IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
